# Contribution-first expert-approved gold annotation — blank template
#
# The original 50-paper migration uses an independent PDF-only AI draft,
# reconciliation against the legacy note, and paper-level expert approval.
# Never use the production contribution extractor, its runs, or scorecards as
# gold input. PDF locators remain the normative scientific evidence.

schema:
  name: benchmark.hvs_contribution_annotation
  version: 1
arxiv_id: ""              # must match the directory name
annotator: ""             # your short handle, stable across the project
annotated_at: ""          # date you finished this paper
guideline_version: ""     # version of the approved contribution guideline
evidence_basis: pdf
annotation_process:
  protocol: contribution_migration_ai_assisted_v1
  preannotation_agent: ""
  preannotation_model: ""
  reconciliation_agent: ""
  reconciliation_model: ""
  expert_review_scope: paper_level
  process_note: ""
status: contributions_found  # or: no_contributions (then leave contributions empty)

contributions:
  - identifiers:          # REQUIRED unordered set; no preferred-name meaning
      - value: ""         # copy exactly as printed; never add a Gaia prefix
        evidence:         # PDF evidence containing this identifier
          - location: ""
            quote: ""
    contribution_type: "" # REQUIRED: candidates_found | follow_up
    contribution_summary: "" # REQUIRED: what this paper actually did to this
                             # object, including unstructured results; for
                             # not_assessed state that no new boundness
                             # conclusion was reported
    contribution_evidence:            # REQUIRED, at least one PDF locator
      - location: ""      # e.g. "Sec 4.2, second paragraph"
        quote: ""         # optional short verbatim quote
    paper_boundness:
      status: ""          # REQUIRED: unbound | possibly_unbound | bound |
                           # no_overall_conclusion | not_assessed
      evidence:            # at least one locator unless status is not_assessed
        - location: ""
          quote: ""
    quantities:            # grouped multivalue; omit for no structured values
      - quantity: ""       # one of the 19 structured quantities, e.g.
                            # observed_phase_space.distance
        values:
          - value: ""      # number as printed; empty only for limit_kind range
            error: ""
            lower_error: ""
            upper_error: ""
            unit: ""       # probabilities: empty with 0--1, or % with 0--100
            limit_kind: none  # none | lower_limit | upper_limit | range
            range_lower: ""  # only for limit_kind: range
            range_upper: ""
            coordinate_format: ""  # ra/dec only: decimal_degrees |
                                   # sexagesimal_hms | sexagesimal_dms |
                                   # sexagesimal_colon
            condition: ""   # potential/prior/method/epoch/release/frame
            paper_preferred: null  # true | false | null (explicit only)
            source: this_paper  # this_paper | prior_work | unclear
            evidence:        # direct PDF evidence for this value
              - location: ""
                quote: ""
            context_evidence: []
            source_note: "" # optional paper-visible source/citation detail

reviewed_exclusions:       # audit material for expert transparency
  - reason: ""             # why this exclusion is scientifically relevant
    evidence:
      - location: ""
        quote: ""
